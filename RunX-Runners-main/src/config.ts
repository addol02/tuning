import * as interface_ from "./utils/interface";

const configs: interface_.Configs = {
    bunHashType: {
        algorithm: "argon2d",
        memoryCost: 4, 
        timeCost: 3, 
    }
}

export default configs;